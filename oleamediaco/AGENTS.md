# AGENTS.md

Sub-project working preferences for `oleamediaco` under `/Users/ecohen/Dev/Webapps`.

## Parent Hub Contract

- This folder is a sub-project of `/Users/ecohen/Dev/Webapps`.
- When a user-visible version changes in this sub-project, also update the Olea Media Co card in `/Users/ecohen/Dev/Webapps/index.html`.
- The parent hub card should show the same visible version and, for public GitHub Pages links, may include a matching cache-bust query such as `?v=X.Y`.
- Keep edits scoped to this sub-project unless the user asks for broader Webapps work.

## Defaults

- Prefer `rg` and `rg --files` for search.
- Prefer small, direct edits over broad refactors.
- Follow the parent repo versioning SOP in `/Users/ecohen/Dev/Webapps/AGENTS.md`.
